import 'package:flutter/material.dart';

class ChocoPage extends StatelessWidget {
  const ChocoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("This is the Choco Page")));
  }
}
